import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Provider from "./authProvider/Provider";
import Navbar from "./components/shared/Navbar";
import { Toaster } from "react-hot-toast";
import ProviderQuery from "./tanstackqueryProvider/QueryProvider";
import Footer from "./components/shared/Footer";


const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Collage Admission pro",
  description: "Generated by collage admission",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={` antialiased max-w-7xl mx-auto p-4`}
      >
        <Provider>
          <ProviderQuery>
            <Navbar />
            <Toaster />
            {children}
            <Footer/>
          </ProviderQuery>
        </Provider>

      </body>
    </html>
  );
}
